<%- include('../layout/layout', {
  content: `
  <div class="flex flex-col items-center justify-center min-h-screen bg-gray-50 py-12 px-4">
  <div class="bg-white shadow-2xl rounded-2xl p-8 w-full max-w-lg">
    <h1 class="text-4xl font-bold mb-6 text-gray-800 text-center">${program.name}</h1>
    
    <div class="space-y-4">
      <p class="text-gray-700"><span class="font-semibold">Description:</span> ${program.description}</p>
      <p class="text-gray-700"><span class="font-semibold">Duration:</span> ${program.duration}</p>
      <p class="text-gray-700"><span class="font-semibold">Price:</span> $${program.price}</p>
      <p class="text-gray-700"><span class="font-semibold">Curriculum:</span> ${program.curriculum}</p>
      <p class="text-gray-700"><span class="font-semibold">Requirements:</span> ${program.requirements}</p>
    </div>
  </div>
</div>

  `
}) %>